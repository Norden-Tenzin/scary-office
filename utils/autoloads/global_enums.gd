extends Node

# Used by InputComponent
enum ActionInput {
	LeftClick,
	RightClick,
	Up,
	Down,
	Left,
	Right,
	DropLeft,
	DropRight,
	ThrowLeft,
	ThrowRight
}

enum SoundLevel {
	Low,
	Mid,
	High,
}

enum LockKeyType {
	Red,
	Green,
	Blue
}

enum Crosshair {
	Dot,
	HandClosed,
	HandOpen
}

enum Hand {
	Left, 
	Right
}

enum MusicName {
	None
}

enum SceneName {
	SplashScreenManager,
	MainMenu,
	SettingsMenu,
	PauseMenu,
	EndScreen,
}
